const { PrismaClient } = require('@prisma/client'); const prisma = new PrismaClient(); prisma.marketplaceCategory.findFirst().then(cat => { if(!cat) return console.log('No cat'); console.log('Category:', cat.name); console.log('Type of parameters:', typeof cat.parameters); console.log('Is Array?', Array.isArray(cat.parameters)); console.log('Sample keys:', Array.isArray(cat.parameters) && cat.parameters.length > 0 ? Object.keys(cat.parameters[0]) : cat.parameters); console.log('Param count:', Array.isArray(cat.parameters) ? cat.parameters.length : 'N/A'); }).finally(() => prisma.$disconnect());
