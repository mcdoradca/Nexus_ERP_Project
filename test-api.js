const { PrismaClient } = require('@prisma/client'); const prisma = new PrismaClient(); async function run() { try { const docs = await prisma.$queryRaw`SELECT title, COUNT(id) as "chunkCount", MAX("createdAt") as "createdAt" FROM "KnowledgeDocument" GROUP BY title ORDER BY "createdAt" DESC`; const mapped = docs.map(d => ({ ...d, chunkCount: Number(d.chunkCount) })); console.log(JSON.stringify(mapped)); } catch(e) { console.error('ERR', e); } } run();
