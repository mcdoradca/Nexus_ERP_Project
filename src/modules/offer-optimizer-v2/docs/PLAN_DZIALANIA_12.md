# PLAN DZIAŁANIA — ZADANIE 12 (E4a korekta)

1. **Przebudowa schematu i wdrożenie HARD FAIL w kodzie**: Zaktualizuję `responseSchema` dla Agenta 1 w orkiestratorze, dodając kluczowe węzły (m.in. `eu_responsible_person`, `sds_required`, `missing_critical_data_reason`) i usuwając pola routingu, zgodnie z instrukcjami z `Agent_1_prompt_v4.md`. Zaimplementuję regułę w kodzie weryfikującą obecność danych odpowiedzialnego podmiotu i fraz CLP – jeśli ich zabraknie, włączony zostanie stan `HALTED_HITL_REQUIRED` z powodem `MISSING_EU_RESPONSIBLE_PERSON` / `MISSING_SDS`.
2. **Synchronizacja stanu i filtrowanie źródeł**: Zapewnię, że orkiestrator zachowuje fazę `PHASE_1_GROUNDING` po ukończeniu A1, wymusza poprawne `pipeline_id` (wstrzyknięcie wartości do promptu LLM i zignorowanie ew. zmyślonego ID), oraz filtruje zakazane domeny P3 (np. allegro, amazon) z listy `research_sources_used`.
3. **Rejestr Decyzji i Przebieg Kontrolny**: Zaktualizuję plik `DECISION_LOG.md` za pomocą Node.js (bezpośredni zapis UTF-8), wprowadzając punkty D15 (zasady cięcia payloadu PIM) oraz D16 (rzutowanie `agentId`). Na koniec uruchomię skrypt testowy, weryfikując poprawność odpowiedzi modelu i wygeneruję raport.
