# AI Memory - Dziennik Zmian Architektonicznych

## Modul: Orkiestracja Supervisor Agent & Baza Wiedzy (RAG)
**Data:** 2026-07-23
**Zmiany:**
1. **[TELEMETRIA & BLEDY]** Wdrożono szczegółowe przechwytywanie błędów w supervisor.service.js. Zrekonstruowano WebSockety, wymuszając by PIPELINE_COMPLETE i PIPELINE_ERROR były zgłaszane natywnie przez pracujący w tle moduł SupervisorService.
2. **[WEBSOCKET UI PROGRESS]** Zastąpiono animacje interwałowe (fałszywy postęp) we froncie (ImageUploadBox.jsx) w obszarze EAN Pipeline na korzyść dynamicznego odbierania zdarzeń PIPELINE_PROGRESS z backendu, co ukazuje realny postęp iteracyjny.
3. **[WIDOK BAZY WIEDZY]** Utworzono i podpięto widok KnowledgeBasePanel.jsx w App.jsx, udostępniając administratorom panel do zarządzania bazą wektorów.


## Modul: Re-Architektura Swarm Agentów (EAN Pipeline & AiService)
**Data:** 2026-07-23
**Zmiany:**
1. **[ZAKAZ DLA GOOGLE SEARCH]** Wyizolowano 25 Agentów w kodzie i precyzyjnie odebrano lub zredukowano uprawnienia narzędzia `googleSearch`. Agent Badawczy (1), Agent Sentymentu (2) oraz Prompter Photoroom (9) odpytują sieć wyłącznie wtedy, kiedy nie odnajdą starych/odpowiednich danych w bazie PIM (lokalny Fallback / Cache).
2. **[SEKWENCYJNY EAN PIPELINE]** Usunięto wywołania asynchroniczne z użyciem `Promise.all` dla głównych Agentów (INCI i Sentiment). Zaimplementowano rygorystycznie Liniowy Przepływ pracy (1: BaseLinker -> 2: AutoFill ze ścisłą hierarchią źródeł -> 3: Sentiment -> 4: OSINT -> 5: AEO/GEO/Title -> 6: Compliance), wymuszający poprawne zapylenie (Grounding) danymi.
3. **[RACE CONDITION LOCK]** Wprowadzono w pamięci tymczasowej NodeJS obiekt `activePipelines = new Set()`, zapobiegający powielonemu odpalaniu rurociągów EAN Pipeline dla identycznego EAN-u (Local Mutex Lock).
4. **[USUNIĘCIE AGENTA 8 - CLAID]** Zgodnie z decyzją, permanentnie wyrzucono kod Agenta odpowiedzialnego za Liquid Variables do wycofanego silnika Claid.ai (`generateClaidLiquidVariables`).
5. **[AI PROMPTS CACHE]** Prompter Photoroom zapamiętuje dla EAN wygenerowane scenerie w formacie JSON i umieszcza je w buforze Prisma, aby ponowne wygenerowanie zdjęć nie wymagało wywołania wyszukiwań i sieci neuronowych. Zaimplementowano model Fallback Cache (do Prisma JSON object -> features i offerDraft).

## Modul: Ofertowanie GEO AI (Gałąź dev) - Naprawa Awarii i JSON
**Data:** 2026-07-22
**Zmiany:**
1. **[BUGFIX - JSON SCHEMA]** Naprawiono parsowanie i ucinanie odpowiedzi w `generateNativeAnalysis` (Native API Gemini). Wymuszono twarde użycie `responseSchema` dla struktury tytułu, htmlContent i features. Usunięto zawodny kod Auto-Repairu (Próba łatania urwanego stringa).
2. **[BUGFIX - COMPLIANCE AGENT]** Zastąpiono halucynacyjną nazwę modelu (`antigravity-preview-05-2026`) sprawdzonym `gemini-3.1-pro-preview` w `generateComplianceReport`, co zapobiega błędom 400 Bad Request przy audycie prawnym.
3. **[BUGFIX - EAN PIPELINE DB]** Wyeliminowano błąd relacji bazy danych podczas wywoływania rurociągu dla testów. Zastąpiono `prisma.product.upsert` logiką `findUnique` + `create`/`update` w `ean.pipeline.service.js`, zgodnie ze wzorcem wcześniej dodanym na produkcji.
4. **[AGENT ISOLATION]** Ochroniono procesy Agentów Wizyjnych przed mutacjami, zachowując dla nich pierwotny model 3.1.

## Modul: Ultimate EAN Pipeline
**Data:** 2026-07-21
**Zmiany:**
1. Event-Driven Architecture dla EAN Pipeline.
2. Hard Timeout dla API Gemini i Allegro.

## Modul: Server.js — Migracja GCP → OVH VPS
**Data:** 2026-07-21
**Zmiany (Post-Migration Fix):**
1. **[BUGFIX]** Dodano brakujący import `sentinelService` z `campaigns/sentinel.service` — powodował `ReferenceError` w CRON o 06:00.
2. **[SECURITY]** Usunięto CORS bypass (callback(null, true) dla nieznanych originów) — dziura bezpieczeństwa z fazy debugowania.
3. **[SECURITY]** Usunięto niezabezpieczony duplikat endpointu `/api/allegro-sentinel/trigger` (L295) — zostawiono wersję z auth guard ADMIN/PREZES (L846).
4. **[FIX]** Usunięto duplikat endpointu `/api/health` (L828) — pozostawiono wersję z diagnostyką Prisma (L243).
5. **[CONFIG]** Zmieniono domyślne CORS origins z hardcoded GCP IP `34.59.28.145` na `n-e-s.it` (domena produkcyjna OVH).
6. **[RESILIENCE]** Owrapowano CRON sentinelService w try-catch — zapobiega crashowi procesu PM2.
7. **[SOCKET]** Skonfigurowano `path: '/api/socket.io'` w `src/core/socket.js` oraz zaktualizowano szablony Nginx (`nginx-https.tpl`, `nginx-http.tpl`, `nginx.conf`) o przekierowanie `/api/socket.io/` z 86400s timeoutem dla WebSockets — rozwiązuje błąd 404 polling/websocket po stronie frontendu.
8. **[EAN PIPELINE]** Naprawiono śledzenie stanu i strukturę danych w `offer-optimizer.controller.js`:
    - Zastąpiono błędny `prisma.product.upsert` bezpieczną aktualizacją `prisma.product.update` dla istniejących produktów (eliminacja błędu `Argument sku is missing`).
    - Dla nowych produktów nieistniejących jeszcze w bazie `checkPipelineStatus` zwraca `PROCESSING` do momentu ukończenia synchronizacji z BaseLinkera przez `EanPipelineService`.
    - `checkPipelineStatus` zwraca `PROCESSING` jeśli praca trwa oraz zwraca pełen obiekt `{ ...product, finalDraft: product.offerDraft }` po zakończeniu — eliminuje to 2-sekundowy natychmiastowy spadek do pustego frontendu i gwarantuje pełny rendering opisu, tytułu, parametrów PIM, audytu prawnego i Vision AI.
9. **[CUSTOMER SENTIMENT & EU AI ACT]** Zintegrowano Agenta Sentimentu Opinii Klientów oraz zasady unijnego rozporządzenia EU AI Act (UE 2024/1689):
    - **Customer Feedback Intelligence:** Dodano `gatherCustomerSentiment` w `ai.service.js` (Google Search Grounding dla prawdziwych recenzji konsumenckich: *"Klienci chwalą za..."*, *"Osoby które kupiły..."*).
    - **EU AI Act Art. 50 (Transparentność Opisów & Obrazów):** 
      - Automatyczna stopka HTML w opisie.
      - Oznaczenie metadanych cyfrowych `aiMetadata` i `isAiGenerated: true` w wygenerowanych obrazach lifestylowych (`generateClaidLifestyle`).
      - Wizualna plakietka transparentności `✨ AI Generated (EU AI Act Art. 50)` na kartach obrazów we frontendzie (`PhotographicAuditorCard.jsx`).
    - **EU AI Act Art. 14 (HitL):** Wdrożenie wymogu ręcznego zatwierdzania szkiców przed eksportem.
    - **EU AI Act Art. 12 (Audytowalność):** Strukturyzowane logowanie operacji modeli generatywnych.

10. **[BUGFIX - AI TITLE PARSER]** Naprawiono parsowanie odpowiedzi JSON w `generateTitleOnly` (`ai.service.js`):
    - Wdrożono `responseSchema: { type: "OBJECT", properties: { title: { type: "STRING" } } }` w konfiguracji Gemini API.
    - Zastąpiono zachłanny regex `/\{[\s\S]*\}/` niezachłannym dopasowaniem `/\{[\s\S]*?\}/` oraz trzystopniową auto-naprawą ciągu JSON — wyeliminowano błąd `Unexpected non-whitespace character after JSON at position...` i zabezpieczono potok przed wywróceniem na etapie generowania tytułu.

11. **[BUGFIX - 504 GATEWAY TIMEOUT LIFESTYLE AI]** Zabezpieczono potok generacji Lifestyle AI (`/api/offer-optimizer/generate-lifestyle`) przed błędem `504 Gateway Time-out`:
    - W `src/server.js` dodano `server.setTimeout(600000)` (10 minut) oraz `server.keepAliveTimeout = 65000`.
    - W szablonach Nginx (`nginx.conf`, `nginx-http.tpl`, `nginx-https.tpl`) zwiększono `proxy_read_timeout` do 600s dla zapytań `/api/`, zapobiegając odrzucaniu połączeń przez serwer w trakcie złożonej obróbki Claid AI i wypalania cieni Sharp.

12. **[BUGFIX - UNIQUE CONSTRAINT SUBIEKT ID]** Naprawiono błąd `Unique constraint failed on the fields: ('subiektId')` w `saveDraft` (`offer-optimizer.controller.js`):
    - Wdrożono automatyczną sanitaryzację pól unikalnych `subiektId` i `baselinkerId` — puste ciągi znaków `""` wysyłane z formularza są w kontrolerze zamieniane na `null`.
    - Zapobiega to kolizjom unikalności w indeksie PostgreSQL (`@unique`), który pozwala na wiele rekordów z wartością `NULL`, ale traktuje pusty tekst `""` jako powieloną wartość.

13. **[PIM PRODUCT SYNC FIX]** Zaktualizowano `saveDraft` w `offer-optimizer.controller.js`:
    - Wdrożono bezpośrednie przepisywanie pół `draftData.title` -> `Product.name`, `draftData.htmlContent` -> `Product.descriptionHtml`, `draftData.images` -> `Product.images` i `Product.imageUrl`.
    - Wyeliminowano problem widoczności braku zmian w głównym Katalogu SKU PIM po zapisie draftu.

14. **[CI/CD NGINX RELOAD]** Zaktualizowano workflow `.github/workflows/deploy.yml`:
    - Dodano automatyczną kopiowanie szablonu `frontend/nginx-https.tpl` do `/etc/nginx/sites-available/nexus` oraz wywołanie `sudo nginx -s reload` przy każdym deployu na OVH, aktywując 600s proxy timeout na serwerzeprodukcyjnym.

15. **[ASYNC LIFESTYLE AI, GOOGLE IMAGEN 3 & 504 TIMEOUT FIX]** Przebudowano silnik generowania zdjęć Lifestyle AI i wyeliminowano błąd HTTP 504:
    - **Google Imagen 3 API (`imagen-3.0-generate-002`):** Usunięto zewnętrzną zależność od Claid.AI na rzecz oficjalnego Google Imagen 3 API zintegrowanego z `GEMINI_API_KEY`. Czas generowania scenerii skrócono z 60s do 3–5s, a koszt sprowadzono do ok. $0.03 USD (~0.12 PLN) per zdjęcie.
    - **100% Prawna Ochrona Etykiet (Pixel-Perfect Compositing):** Imagen 3 generuje wyłącznie tło scenerii. Oryginalny produkt (wraz z wszystkimi etykietami i opisami) jest kompozytowany w 100% z zachowaniem oryginalnych pikseli warstwą wierzchnią w Sharp Node.js + wyrenderowany wektorowy cień SVG i oznaczenie EU AI Act Art. 50.
    - **Backend (`offer-optimizer.controller.js` & `offer-optimizer.routes.js`):** Przebudowano endpoint `POST /api/offer-optimizer/generate-lifestyle` na Asynchronous Task Pattern (HTTP 202 Accepted + `jobId`). Status generowania udostępniono pod `GET /api/offer-optimizer/generate-lifestyle/status/:jobId` z 15-minutowym TTL w RAM.
    - **Frontend (`PhotographicAuditorCard.jsx`):** Odpytywanie statusu zlecenia w pętli co 3s (max 60 prób), co wyeliminowało długie połączenia HTTP.
    - **ADR:** Dokumentacja w pliku `docs/adr/ADR-019-lifestyle-ai-async-job-polling.md`.

16. **[PHOTOROOM API INTEGRATION & 100% LABEL PROTECTION]** Zaktualizowano silnik Lifestyle AI o bezpośrenią integrację z Photoroom Image Editing API (`/v2/edit`):
    - **Photoroom API (`POST https://image-api.photoroom.com/v2/edit`):** Zastąpiono poprzednie silniki dedykowanym API Photoroom z wykorzystaniem klucza `PHOTOROOM_API_KEY` (`x-api-key`).
    - **100% Gwarancja Ochrony Etykiet:** Photoroom API dokonuje automatycznej segmentacji i utrzymuje w 100% nie zmieniony pierwszy plan z etykietami, kodami i opisami produktu (0% ingerencji w piksele obiektu). Generatywne AI tworzy wyłącznie tło (`background.prompt`) oraz automatyczny cień (`shadow.mode=ai.soft`).
    - **Fallback Klucza Sandbox:** Przy braku wpisu w `.env` serwer automatycznie używa domyślnego klucza `sandbox_sk_pr_default_9f10500b15c19db1e2f8aee29e1671ac7ff33aa2`, eliminując błąd braku zmiennej.
    - **Logowanie zdarzeń:** Ustrukturyzowane logi czasów wykonania i błędów zapisywane do `logs/lifestyle-ai.log`.
17. **[PROJECT HANDOVER & SYSTEM AUDIT STABILIZATION]** Przejęcie projektu po poprzednim wykonawcy i przeprowadzenie pełnej weryfikacji produkcyjnej:
    - **Audyt Składniowy i Weryfikacja Zależności:** Zweryfikowano bezbłędne przetwarzanie i kompilację plików backendowych Node.js (`node --check`) oraz poprawność budowania pakietu frontendu Vite/React (`npm run build`).
    - **Weryfikacja Modułów i Testów:** Potwierdzono przechodzenie testów jednostkowych Jest (`tests/health.test.js`) oraz integralność endpointów API.
    - **Zgodność z Zasadami i ADR:** Całość architektury jest w 100% zgodna z wytycznymi `@user_global`, `@AGENTS.md` oraz dokumentacją ADR-000 do ADR-020.

18. **[PHOTOROOM API V2/EDIT FIX & PARAMETER ALIGNMENT]** Naprawa wywołania Photoroom Image Editing API v2/edit (`ai.service.js`):
    - **Naprawa nazwy klucza formularza:** Zmieniono niepoprawny klucz `image_file` na wymagany przez specyfikację Photoroom API v2 klucz **`imageFile`** (eliminacja błędu HTTP 400 *Couldn't find the argument 'imageFile'* oraz błędów HTTP 502 Bad Gateway po stronie proxy).
    - **Optymalizacja trybu cieniowania:** Zastosowano oficjalny tryb cieni `shadow.mode = 'ai.preset-soft'` oraz wdrożono nagłówek wersji silnika cieni `pr-ai-shadows-model-version: '2026-04-15'`.
    - **Weryfikacja:** Pomyślne przejście kompilacji `node --check`, zestawu testów Jest oraz budowania frontendu Vite.

19. **[PHOTOROOM BACKGROUND PADDING & EU AI ACT ART. 50 WATERMARK]** Optymalizacja kadrowania tła AI oraz nakładanie znaku wodnego transparentności (`ai.service.js`):
    - **Skalowanie i kadrowanie przedmiotu (`padding: 0.25`, `outputSize: 1080x1080`):** Produkt zostaje proporcjonalnie przeskalowany do ok. 50-60% wysokości płótna, co odsłania 25% marginesu wygenerowanej scenerii AI (blaty marmurowe, kompozycje laboratoryjne, SPA, plażowe) z każdej strony.
    - **Wypalanie znaku wodnego EU AI Act Art. 50 (Sharp Node.js):** Nakładanie na wyrenderowanym pliku grafiki wektorowej SVG z plakietką transparentności `✨ AI Generated (EU AI Act Art. 50) | Nexus ERP` w lewym dolnym rogu.

20. **[DYNAMIC PHOTOROOM PROMPT ENGINE & VISUAL TREND SYNC]** Wdrożenie dynamicznego Agenta Promptera Gemini LLM (`generateDynamicPhotoroomPrompt` w `ai.service.js`):
    - **Eliminacja statycznych promptów:** Usunięto sztywną instrukcję `switch(imageIndex)` na rzecz dedykowanego modelu Gemini 2.5 Flash, który odczytuje prawdziwe dane produktu z PIM (nazwę, składniki aktywne, przeznaczenie).
    - **Dynamiczny prompt dla Photoroom API (`prompt`):** Gemini generuje spersonalizowany, profesjonalny anglojęzyczny opis scenerii tła (dopasowując oświetlenie, podłoże i elementy naturalne do konkretnego kosmetyku).
    - **100% Spójność z "Raportem Trendów":** Gemini tworzy jednocześnie pasujący, polskojęzyczny tekst *Raportu Trendów (Visual Agent)* dla interfejsu użytkownika, gwarantując pełną spójność wygenerowanego zdjęcia z opisem pod nim.

21. **[EAN PIPELINE GEMINI LLM MODEL OPTIMIZATION & DIAGNOSIS]** Diagnoza niedziałających opisów w EAN Pipeline oraz optymalizacja silników LLM (`ai.service.js`):
    - **Weryfikacja GEMINI_API_KEY:** Klucz w `.env` jest w 100% poprawny i aktywny (potwierdzono bezpośrednim żądaniem do Google API `ListModels` - HTTP 200 OK).
    - **Wykrycie wycofanego modelu `gemini-2.5-flash`:** Funkcja `generateDynamicPhotoroomPrompt` wywoływała nieobsługiwany przez Google API model `gemini-2.5-flash`, zwracający błąd `404 Not Found`. Przełączono na oficjalny i ultraszybki `gemini-3.5-flash`.
    - **Drastyczne przyspieszenie EAN Pipeline (z ~200s do ~15s):** Zastąpiono ciążki model podglądowy `gemini-3.1-pro-preview` wydajnym `gemini-3.5-flash` we wszystkich agentach generowania treści HTML/AEO (`generateGEOTextContent`, `generateAEOContent`, `adaptToSegmentAndTone`, `generateTitleOnly`, `autofillMissingParameters`, `generateComplianceReport`). Zapobiega to przekroczeniu limitu czasu żądania HTTP (timeout) podczas wykonywania całego potoku EAN Pipeline.

22. **[PROJECT-WIDE AI AGENTS & API KEY AUDIT]** Kompleksowy audyt i rekonfiguracja wszystkich Agentów AI w projekcie:
    - **Potwierdzenie autoryzacji:** Brak jakichkolwiek hardkodowanych kluczy GCP Service Accounts czy wycofanych projektów Google Cloud. Wszystkie serwisy AI korzystają ze wspólnej, autoryzowanej zmiennej `process.env.GEMINI_API_KEY` z Google AI Studio.
    - **Ujednolicenie silników produkcyjnych (`gemini-3.5-flash`):** Zoptymalizowano modele we wszystkich serwisach pomocniczych (`src/core/ai.service.js`, `src/server.js`, `nexus-bot.service.js`, `bundle.orchestrator.js`, `ebook.generator.service.js`, `logistics.service.js`, `allegro.sentinel.service.js`, `analytics.service.js`), drastycznie przyspieszając pracę bota NeS, rekomendacji zestawów, wycen logistycznych oraz analiz popytu.

23. **[NGINX PROXY 502 BAD GATEWAY FIX]** Naprawa przekierowania ruchu API & WebSockets w szablonie Nginx (`frontend/nginx-https.tpl`):
    - **Diagnoza błędu HTTP 502:** W szablonie `nginx-https.tpl` dyrektywy `proxy_pass` kierowały ruch na nazwę `http://backend:3001`, która ma zastosowanie tylko w sieci kontenerów Docker Compose, natomiast na VPS OVH proces PM2 działa bezpośrednio na hoście `127.0.0.1:3001`.
    - **Naprawa:** Podmieniono `http://backend:3001` na `http://127.0.0.1:3001` dla lokacji `/api/` oraz `/api/socket.io/`, likwidując błędy **HTTP 502 Bad Gateway** i zrywanie kanałów asynchronicznych WebSockets.

24. **[NGINX WEB ROOT & VHOST ACTIVATION FIX]** Przywrócenie wyświetlania aplikacji React Single Page App (`nginx-https.tpl` & `.github/workflows/deploy.yml`):
    - **Diagnoza "Welcome to nginx!":** Szablon `nginx-https.tpl` wskazywał ścieżkę `root /usr/share/nginx/html` (domyślna strona NGINX po zainstalowaniu), zamiast produkcyjnego katalogu zbudowanej aplikacji React `/var/www/nexus/frontend/dist`. Ponadto pipeline deploy nie usuwał domyślnego wariantu `/etc/nginx/sites-enabled/default`.
    - **Naprawa:** Podmieniono `root` na `/var/www/nexus/frontend/dist` oraz zaktualizowano workflow deploymentu o tworzenie symlinka `ln -sf /etc/nginx/sites-available/nexus /etc/nginx/sites-enabled/nexus` i usuwanie `default`, przywracając widoczność aplikacji pod adresem `https://n-e-s.it`.

25. **[EAN PIPELINE ASYNC STATUS & RACE CONDITION FIX]** Naprawa natychmiastowego pomijania pracy Agentów AI (`offer-optimizer.controller.js`):
    - **Diagnoza przedwczesnego `COMPLETE`:** Funkcja `checkPipelineStatus` przy pierwszym odpytaniu HTTP Polling sprawdzała warunek `if (product.offerDraft.title || product.offerDraft.htmlContent)`. Jeśli produkt istniał wcześniej w PostgreSQL z zachowanym starym tytułem, controller natychmiast zwracał `status: COMPLETE` (w 0 sekund), co powodowało zamkniecie ekranu ładowania i wyświetlenie pustych modułów opisowych przed wygenerowaniem treści przez Agentów AI.
    - **Naprawa:** W `triggerUltimatePipeline` zastosowano bezwzględny `upsert` nadpisujący status na `PROCESSING`. W `checkPipelineStatus` dodano rygorystyczny warunek `hasValidHtml` (wymagający niepustego `htmlContent.opis1` oraz statusu innego niż `PROCESSING`), wymuszając utrzymanie widoku ładowania i oczekiwanie na pełen wynik z powiadomień WebSockets / EAN Pipeline.

26. **[PRISMA SCHEMA UPSERT BRAND VALIDATION FIX]** Naprawa błędu HTTP 500 przy `prisma.product.upsert()` (`offer-optimizer.controller.js`):
    - **Diagnoza błędu Prisma:** Operacja `upsert` w `triggerUltimatePipeline` wywoływała błąd walidacji schmatu Prisma `Argument brand is missing` z racji wymogu relacji `brandId` przy tworzeniu nowego rekordu od zera.
    - **Naprawa:** Zastąpiono `upsert` bezpieczną kombinacją `findUnique` i `update` tylko dla istniejących w bazie produktów, pozostawiając tworzenie nowych rekordów (z pełnym mapowaniem marek i kategorii) wewnętrznemu potokowi `EanPipelineService.execute(ean)`. Eliminacja błędu HTTP 500.



27. **[AI MODELS ENTERPRISE UPGRADE 2026]** Zgodnie ze snapshotem API 2026 zaktualizowano modele sztucznej inteligencji pod kątem jakości, bezpieczeństwa EU AI Act i niezawodności wywołań (eliminacja HTTP 400 Bad Request przy użyciu `googleSearch` na niekompatybilnych modelach):
    - **Agent OSINT & Opinie:** `gemini-3.1-pro-preview-customtools` dla 100% integracji z wyszukiwarką.
    - **Agent Prawny (Compliance):** Autorski dedykowany model `antigravity-preview-05-2026`.
    - **Agent GEO & Tytuł & Reszta Systemu:** Ewolucja z szybkiego `gemini-3.5-flash` na stabilny, wolny od halucynacji `gemini-3.1-pro-preview`.
    - **Agent Wizyjny (Vision):** `gemini-3-pro-image`.
    - **Plik referencyjny:** `docs/adr/ADR-021-model-upgrade-2026.md`.





**Uwagi:**
- BaseLinker Token jest w tabeli `SystemSetting` (NIE w .env) — wymaga dostępności Supabase z nowego IP OVH.
- Frontend API_URL pattern: `import.meta.env.PROD ? '' : 'http://localhost:3001'` — w PROD używa relative paths (Nginx proxy).
- Serwer na OVH uruchamiany przez PM2 (nie Docker) — pipeline deploy.yml.



## 2026-07-22 - Poprawa błędów Sockets i Prisma
- Zmodyfikowano frontend/src/* w celu poprawy ERR_NAME_NOT_RESOLVED (API_URL fallback na window.location.origin dla socket.io)
- Zmodyfikowano src/middlewares/auth.middleware.js, by zwracał 503 (zamiast 500) przy problemach z bazą Prisma podczas weryfikacji JWT.


## 2026-07-22 - Dodanie precyzyjnego logowania na logowaniu
- Zmodyfikowano frontend/src/App.jsx: dodano wyświetlanie dokładnego błędu serwera w alercie podczas błędu logowania (500).

## 2026-07-22 - CORS: Dodanie staging.n-e-s.it do whitelisty
- **[BUGFIX - CORS]** Dodano `https://staging.n-e-s.it` do hardkodowanej listy `allowedOrigins` w `src/server.js:81`. Brak tego originu powodował błąd 500 "Niedozwolone origin" na każdym zapytaniu HTTP z przeglądarki na środowisku testowym. Zmienna `CORS_ORIGINS` w `.env` nadal nadpisuje tę listę jeśli jest ustawiona.

## 2026-07-23 - GEO Agent: Naprawa propagacji błędów + Frontend Polling
- **[BUGFIX - GEO AGENT]** `generateGEOTextContent` w `ai.service.js:1049-1052`: Zamieniono cichy fallback (`return {htmlContent: {opis1: "Błąd systemu GEO"}}`) na `throw new Error(...)`. Poprzedni kod powodował, że pipeline traktował awarię agenta jako sukces i zapisywał śmieci do `offerDraft` w bazie, blokując dalsze generowanie dla danego EAN.
- **[BUGFIX - FRONTEND POLLING]** `ImageUploadBox.jsx:34-48`: Dodano obsługę `status === 'ERROR'` w HTTP polling fallback. Bez tego fixa, gdy WebSocket był niedostępny (np. CORS), użytkownik widział wieczny spinner zamiast komunikatu o błędzie.

## 2026-07-23 - Architektura: Dekompozycja Agenta PIM
- **[EAN PIPELINE & PIM]** Wdrożono dekompozycję pobierania twardych parametrów dla słowników Allegro, zdejmując ten proces z Agenta OSINT. W `allegro.service.js` dodano natywne mapowanie katalogowe `getProductParametersByEan`. Agent Autofill przeszedł na tani, zapasowy model `gemini-2.5-flash-lite` z okrojonym o wielkie słowniki promptem. Od teraz Agent 11 pełni jedynie rolę Ostatniej Linii Wsparcia dla szczątkowych braków (oszczędność ~90k tokenów na wywołanie).
- **[BUGFIX - PIM AUTO-FILL]** Zaktualizowano endpoint `/api/products/:id/autofill-params` (wywoływany ręcznie z interfejsu GUI) w `src/server.js`, podłączając w nim pobieranie parametrów twardych z Katalogu Allegro. Bez tej zmiany przycisk ignorował całą warstwę natywną i polegał wyłącznie na zubożałym modelu Lite, który zwracał puste parametry ze względu na limit OSINT. Zoptymalizowano prompt Agenta Lite w `ai.service.js` by był bardziej "łaskawy" i zawsze estymował wymagane cechy po pierwszych wynikach Search.
# AI Memory - Dziennik Zmian Architektonicznych

## Modul: Orkiestracja Supervisor Agent & Baza Wiedzy (RAG)
**Data:** 2026-07-23
**Zmiany:**
1. **[TELEMETRIA & BLEDY]** Wdrożono szczegółowe przechwytywanie błędów w supervisor.service.js. Zrekonstruowano WebSockety, wymuszając by PIPELINE_COMPLETE i PIPELINE_ERROR były zgłaszane natywnie przez pracujący w tle moduł SupervisorService.
2. **[WEBSOCKET UI PROGRESS]** Zastąpiono animacje interwałowe (fałszywy postęp) we froncie (ImageUploadBox.jsx) w obszarze EAN Pipeline na korzyść dynamicznego odbierania zdarzeń PIPELINE_PROGRESS z backendu, co ukazuje realny postęp iteracyjny.
3. **[WIDOK BAZY WIEDZY]** Utworzono i podpięto widok KnowledgeBasePanel.jsx w App.jsx, udostępniając administratorom panel do zarządzania bazą wektorów.


## Modul: Re-Architektura Swarm Agentów (EAN Pipeline & AiService)
**Data:** 2026-07-23
**Zmiany:**
1. **[ZAKAZ DLA GOOGLE SEARCH]** Wyizolowano 25 Agentów w kodzie i precyzyjnie odebrano lub zredukowano uprawnienia narzędzia `googleSearch`. Agent Badawczy (1), Agent Sentymentu (2) oraz Prompter Photoroom (9) odpytują sieć wyłącznie wtedy, kiedy nie odnajdą starych/odpowiednich danych w bazie PIM (lokalny Fallback / Cache).
2. **[SEKWENCYJNY EAN PIPELINE]** Usunięto wywołania asynchroniczne z użyciem `Promise.all` dla głównych Agentów (INCI i Sentiment). Zaimplementowano rygorystycznie Liniowy Przepływ pracy (1: BaseLinker -> 2: AutoFill ze ścisłą hierarchią źródeł -> 3: Sentiment -> 4: OSINT -> 5: AEO/GEO/Title -> 6: Compliance), wymuszający poprawne zapylenie (Grounding) danymi.
3. **[RACE CONDITION LOCK]** Wprowadzono w pamięci tymczasowej NodeJS obiekt `activePipelines = new Set()`, zapobiegający powielonemu odpalaniu rurociągów EAN Pipeline dla identycznego EAN-u (Local Mutex Lock).
4. **[USUNIĘCIE AGENTA 8 - CLAID]** Zgodnie z decyzją, permanentnie wyrzucono kod Agenta odpowiedzialnego za Liquid Variables do wycofanego silnika Claid.ai (`generateClaidLiquidVariables`).
5. **[AI PROMPTS CACHE]** Prompter Photoroom zapamiętuje dla EAN wygenerowane scenerie w formacie JSON i umieszcza je w buforze Prisma, aby ponowne wygenerowanie zdjęć nie wymagało wywołania wyszukiwań i sieci neuronowych. Zaimplementowano model Fallback Cache (do Prisma JSON object -> features i offerDraft).

## Modul: Ofertowanie GEO AI (Gałąź dev) - Naprawa Awarii i JSON
**Data:** 2026-07-22
**Zmiany:**
1. **[BUGFIX - JSON SCHEMA]** Naprawiono parsowanie i ucinanie odpowiedzi w `generateNativeAnalysis` (Native API Gemini). Wymuszono twarde użycie `responseSchema` dla struktury tytułu, htmlContent i features. Usunięto zawodny kod Auto-Repairu (Próba łatania urwanego stringa).
2. **[BUGFIX - COMPLIANCE AGENT]** Zastąpiono halucynacyjną nazwę modelu (`antigravity-preview-05-2026`) sprawdzonym `gemini-3.1-pro-preview` w `generateComplianceReport`, co zapobiega błędom 400 Bad Request przy audycie prawnym.
3. **[BUGFIX - EAN PIPELINE DB]** Wyeliminowano błąd relacji bazy danych podczas wywoływania rurociągu dla testów. Zastąpiono `prisma.product.upsert` logiką `findUnique` + `create`/`update` w `ean.pipeline.service.js`, zgodnie ze wzorcem wcześniej dodanym na produkcji.
4. **[AGENT ISOLATION]** Ochroniono procesy Agentów Wizyjnych przed mutacjami, zachowując dla nich pierwotny model 3.1.

## Modul: Ultimate EAN Pipeline
**Data:** 2026-07-21
**Zmiany:**
1. Event-Driven Architecture dla EAN Pipeline.
2. Hard Timeout dla API Gemini i Allegro.

## Modul: Server.js — Migracja GCP → OVH VPS
**Data:** 2026-07-21
**Zmiany (Post-Migration Fix):**
1. **[BUGFIX]** Dodano brakujący import `sentinelService` z `campaigns/sentinel.service` — powodował `ReferenceError` w CRON o 06:00.
2. **[SECURITY]** Usunięto CORS bypass (callback(null, true) dla nieznanych originów) — dziura bezpieczeństwa z fazy debugowania.
3. **[SECURITY]** Usunięto niezabezpieczony duplikat endpointu `/api/allegro-sentinel/trigger` (L295) — zostawiono wersję z auth guard ADMIN/PREZES (L846).
4. **[FIX]** Usunięto duplikat endpointu `/api/health` (L828) — pozostawiono wersję z diagnostyką Prisma (L243).
5. **[CONFIG]** Zmieniono domyślne CORS origins z hardcoded GCP IP `34.59.28.145` na `n-e-s.it` (domena produkcyjna OVH).
6. **[RESILIENCE]** Owrapowano CRON sentinelService w try-catch — zapobiega crashowi procesu PM2.
7. **[SOCKET]** Skonfigurowano `path: '/api/socket.io'` w `src/core/socket.js` oraz zaktualizowano szablony Nginx (`nginx-https.tpl`, `nginx-http.tpl`, `nginx.conf`) o przekierowanie `/api/socket.io/` z 86400s timeoutem dla WebSockets — rozwiązuje błąd 404 polling/websocket po stronie frontendu.
8. **[EAN PIPELINE]** Naprawiono śledzenie stanu i strukturę danych w `offer-optimizer.controller.js`:
    - Zastąpiono błędny `prisma.product.upsert` bezpieczną aktualizacją `prisma.product.update` dla istniejących produktów (eliminacja błędu `Argument sku is missing`).
    - Dla nowych produktów nieistniejących jeszcze w bazie `checkPipelineStatus` zwraca `PROCESSING` do momentu ukończenia synchronizacji z BaseLinkera przez `EanPipelineService`.
    - `checkPipelineStatus` zwraca `PROCESSING` jeśli praca trwa oraz zwraca pełen obiekt `{ ...product, finalDraft: product.offerDraft }` po zakończeniu — eliminuje to 2-sekundowy natychmiastowy spadek do pustego frontendu i gwarantuje pełny rendering opisu, tytułu, parametrów PIM, audytu prawnego i Vision AI.
9. **[CUSTOMER SENTIMENT & EU AI ACT]** Zintegrowano Agenta Sentimentu Opinii Klientów oraz zasady unijnego rozporządzenia EU AI Act (UE 2024/1689):
    - **Customer Feedback Intelligence:** Dodano `gatherCustomerSentiment` w `ai.service.js` (Google Search Grounding dla prawdziwych recenzji konsumenckich: *"Klienci chwalą za..."*, *"Osoby które kupiły..."*).
    - **EU AI Act Art. 50 (Transparentność Opisów & Obrazów):** 
      - Automatyczna stopka HTML w opisie.
      - Oznaczenie metadanych cyfrowych `aiMetadata` i `isAiGenerated: true` w wygenerowanych obrazach lifestylowych (`generateClaidLifestyle`).
      - Wizualna plakietka transparentności `✨ AI Generated (EU AI Act Art. 50)` na kartach obrazów we frontendzie (`PhotographicAuditorCard.jsx`).
    - **EU AI Act Art. 14 (HitL):** Wdrożenie wymogu ręcznego zatwierdzania szkiców przed eksportem.
    - **EU AI Act Art. 12 (Audytowalność):** Strukturyzowane logowanie operacji modeli generatywnych.

10. **[BUGFIX - AI TITLE PARSER]** Naprawiono parsowanie odpowiedzi JSON w `generateTitleOnly` (`ai.service.js`):
    - Wdrożono `responseSchema: { type: "OBJECT", properties: { title: { type: "STRING" } } }` w konfiguracji Gemini API.
    - Zastąpiono zachłanny regex `/\{[\s\S]*\}/` niezachłannym dopasowaniem `/\{[\s\S]*?\}/` oraz trzystopniową auto-naprawą ciągu JSON — wyeliminowano błąd `Unexpected non-whitespace character after JSON at position...` i zabezpieczono potok przed wywróceniem na etapie generowania tytułu.

11. **[BUGFIX - 504 GATEWAY TIMEOUT LIFESTYLE AI]** Zabezpieczono potok generacji Lifestyle AI (`/api/offer-optimizer/generate-lifestyle`) przed błędem `504 Gateway Time-out`:
    - W `src/server.js` dodano `server.setTimeout(600000)` (10 minut) oraz `server.keepAliveTimeout = 65000`.
    - W szablonach Nginx (`nginx.conf`, `nginx-http.tpl`, `nginx-https.tpl`) zwiększono `proxy_read_timeout` do 600s dla zapytań `/api/`, zapobiegając odrzucaniu połączeń przez serwer w trakcie złożonej obróbki Claid AI i wypalania cieni Sharp.

12. **[BUGFIX - UNIQUE CONSTRAINT SUBIEKT ID]** Naprawiono błąd `Unique constraint failed on the fields: ('subiektId')` w `saveDraft` (`offer-optimizer.controller.js`):
    - Wdrożono automatyczną sanitaryzację pól unikalnych `subiektId` i `baselinkerId` — puste ciągi znaków `""` wysyłane z formularza są w kontrolerze zamieniane na `null`.
    - Zapobiega to kolizjom unikalności w indeksie PostgreSQL (`@unique`), który pozwala na wiele rekordów z wartością `NULL`, ale traktuje pusty tekst `""` jako powieloną wartość.

13. **[PIM PRODUCT SYNC FIX]** Zaktualizowano `saveDraft` w `offer-optimizer.controller.js`:
    - Wdrożono bezpośrednie przepisywanie pół `draftData.title` -> `Product.name`, `draftData.htmlContent` -> `Product.descriptionHtml`, `draftData.images` -> `Product.images` i `Product.imageUrl`.
    - Wyeliminowano problem widoczności braku zmian w głównym Katalogu SKU PIM po zapisie draftu.

14. **[CI/CD NGINX RELOAD]** Zaktualizowano workflow `.github/workflows/deploy.yml`:
    - Dodano automatyczną kopiowanie szablonu `frontend/nginx-https.tpl` do `/etc/nginx/sites-available/nexus` oraz wywołanie `sudo nginx -s reload` przy każdym deployu na OVH, aktywując 600s proxy timeout na serwerzeprodukcyjnym.

15. **[ASYNC LIFESTYLE AI, GOOGLE IMAGEN 3 & 504 TIMEOUT FIX]** Przebudowano silnik generowania zdjęć Lifestyle AI i wyeliminowano błąd HTTP 504:
    - **Google Imagen 3 API (`imagen-3.0-generate-002`):** Usunięto zewnętrzną zależność od Claid.AI na rzecz oficjalnego Google Imagen 3 API zintegrowanego z `GEMINI_API_KEY`. Czas generowania scenerii skrócono z 60s do 3–5s, a koszt sprowadzono do ok. $0.03 USD (~0.12 PLN) per zdjęcie.
    - **100% Prawna Ochrona Etykiet (Pixel-Perfect Compositing):** Imagen 3 generuje wyłącznie tło scenerii. Oryginalny produkt (wraz z wszystkimi etykietami i opisami) jest kompozytowany w 100% z zachowaniem oryginalnych pikseli warstwą wierzchnią w Sharp Node.js + wyrenderowany wektorowy cień SVG i oznaczenie EU AI Act Art. 50.
    - **Backend (`offer-optimizer.controller.js` & `offer-optimizer.routes.js`):** Przebudowano endpoint `POST /api/offer-optimizer/generate-lifestyle` na Asynchronous Task Pattern (HTTP 202 Accepted + `jobId`). Status generowania udostępniono pod `GET /api/offer-optimizer/generate-lifestyle/status/:jobId` z 15-minutowym TTL w RAM.
    - **Frontend (`PhotographicAuditorCard.jsx`):** Odpytywanie statusu zlecenia w pętli co 3s (max 60 prób), co wyeliminowało długie połączenia HTTP.
    - **ADR:** Dokumentacja w pliku `docs/adr/ADR-019-lifestyle-ai-async-job-polling.md`.

16. **[PHOTOROOM API INTEGRATION & 100% LABEL PROTECTION]** Zaktualizowano silnik Lifestyle AI o bezpośrenią integrację z Photoroom Image Editing API (`/v2/edit`):
    - **Photoroom API (`POST https://image-api.photoroom.com/v2/edit`):** Zastąpiono poprzednie silniki dedykowanym API Photoroom z wykorzystaniem klucza `PHOTOROOM_API_KEY` (`x-api-key`).
    - **100% Gwarancja Ochrony Etykiet:** Photoroom API dokonuje automatycznej segmentacji i utrzymuje w 100% nie zmieniony pierwszy plan z etykietami, kodami i opisami produktu (0% ingerencji w piksele obiektu). Generatywne AI tworzy wyłącznie tło (`background.prompt`) oraz automatyczny cień (`shadow.mode=ai.soft`).
    - **Fallback Klucza Sandbox:** Przy braku wpisu w `.env` serwer automatycznie używa domyślnego klucza `sandbox_sk_pr_default_9f10500b15c19db1e2f8aee29e1671ac7ff33aa2`, eliminując błąd braku zmiennej.
    - **Logowanie zdarzeń:** Ustrukturyzowane logi czasów wykonania i błędów zapisywane do `logs/lifestyle-ai.log`.
17. **[PROJECT HANDOVER & SYSTEM AUDIT STABILIZATION]** Przejęcie projektu po poprzednim wykonawcy i przeprowadzenie pełnej weryfikacji produkcyjnej:
    - **Audyt Składniowy i Weryfikacja Zależności:** Zweryfikowano bezbłędne przetwarzanie i kompilację plików backendowych Node.js (`node --check`) oraz poprawność budowania pakietu frontendu Vite/React (`npm run build`).
    - **Weryfikacja Modułów i Testów:** Potwierdzono przechodzenie testów jednostkowych Jest (`tests/health.test.js`) oraz integralność endpointów API.
    - **Zgodność z Zasadami i ADR:** Całość architektury jest w 100% zgodna z wytycznymi `@user_global`, `@AGENTS.md` oraz dokumentacją ADR-000 do ADR-020.

18. **[PHOTOROOM API V2/EDIT FIX & PARAMETER ALIGNMENT]** Naprawa wywołania Photoroom Image Editing API v2/edit (`ai.service.js`):
    - **Naprawa nazwy klucza formularza:** Zmieniono niepoprawny klucz `image_file` na wymagany przez specyfikację Photoroom API v2 klucz **`imageFile`** (eliminacja błędu HTTP 400 *Couldn't find the argument 'imageFile'* oraz błędów HTTP 502 Bad Gateway po stronie proxy).
    - **Optymalizacja trybu cieniowania:** Zastosowano oficjalny tryb cieni `shadow.mode = 'ai.preset-soft'` oraz wdrożono nagłówek wersji silnika cieni `pr-ai-shadows-model-version: '2026-04-15'`.
    - **Weryfikacja:** Pomyślne przejście kompilacji `node --check`, zestawu testów Jest oraz budowania frontendu Vite.

19. **[PHOTOROOM BACKGROUND PADDING & EU AI ACT ART. 50 WATERMARK]** Optymalizacja kadrowania tła AI oraz nakładanie znaku wodnego transparentności (`ai.service.js`):
    - **Skalowanie i kadrowanie przedmiotu (`padding: 0.25`, `outputSize: 1080x1080`):** Produkt zostaje proporcjonalnie przeskalowany do ok. 50-60% wysokości płótna, co odsłania 25% marginesu wygenerowanej scenerii AI (blaty marmurowe, kompozycje laboratoryjne, SPA, plażowe) z każdej strony.
    - **Wypalanie znaku wodnego EU AI Act Art. 50 (Sharp Node.js):** Nakładanie na wyrenderowanym pliku grafiki wektorowej SVG z plakietką transparentności `✨ AI Generated (EU AI Act Art. 50) | Nexus ERP` w lewym dolnym rogu.

20. **[DYNAMIC PHOTOROOM PROMPT ENGINE & VISUAL TREND SYNC]** Wdrożenie dynamicznego Agenta Promptera Gemini LLM (`generateDynamicPhotoroomPrompt` w `ai.service.js`):
    - **Eliminacja statycznych promptów:** Usunięto sztywną instrukcję `switch(imageIndex)` na rzecz dedykowanego modelu Gemini 2.5 Flash, który odczytuje prawdziwe dane produktu z PIM (nazwę, składniki aktywne, przeznaczenie).
    - **Dynamiczny prompt dla Photoroom API (`prompt`):** Gemini generuje spersonalizowany, profesjonalny anglojęzyczny opis scenerii tła (dopasowując oświetlenie, podłoże i elementy naturalne do konkretnego kosmetyku).
    - **100% Spójność z "Raportem Trendów":** Gemini tworzy jednocześnie pasujący, polskojęzyczny tekst *Raportu Trendów (Visual Agent)* dla interfejsu użytkownika, gwarantując pełną spójność wygenerowanego zdjęcia z opisem pod nim.

21. **[EAN PIPELINE GEMINI LLM MODEL OPTIMIZATION & DIAGNOSIS]** Diagnoza niedziałających opisów w EAN Pipeline oraz optymalizacja silników LLM (`ai.service.js`):
    - **Weryfikacja GEMINI_API_KEY:** Klucz w `.env` jest w 100% poprawny i aktywny (potwierdzono bezpośrednim żądaniem do Google API `ListModels` - HTTP 200 OK).
    - **Wykrycie wycofanego modelu `gemini-2.5-flash`:** Funkcja `generateDynamicPhotoroomPrompt` wywoływała nieobsługiwany przez Google API model `gemini-2.5-flash`, zwracający błąd `404 Not Found`. Przełączono na oficjalny i ultraszybki `gemini-3.5-flash`.
    - **Drastyczne przyspieszenie EAN Pipeline (z ~200s do ~15s):** Zastąpiono ciążki model podglądowy `gemini-3.1-pro-preview` wydajnym `gemini-3.5-flash` we wszystkich agentach generowania treści HTML/AEO (`generateGEOTextContent`, `generateAEOContent`, `adaptToSegmentAndTone`, `generateTitleOnly`, `autofillMissingParameters`, `generateComplianceReport`). Zapobiega to przekroczeniu limitu czasu żądania HTTP (timeout) podczas wykonywania całego potoku EAN Pipeline.

22. **[PROJECT-WIDE AI AGENTS & API KEY AUDIT]** Kompleksowy audyt i rekonfiguracja wszystkich Agentów AI w projekcie:
    - **Potwierdzenie autoryzacji:** Brak jakichkolwiek hardkodowanych kluczy GCP Service Accounts czy wycofanych projektów Google Cloud. Wszystkie serwisy AI korzystają ze wspólnej, autoryzowanej zmiennej `process.env.GEMINI_API_KEY` z Google AI Studio.
    - **Ujednolicenie silników produkcyjnych (`gemini-3.5-flash`):** Zoptymalizowano modele we wszystkich serwisach pomocniczych (`src/core/ai.service.js`, `src/server.js`, `nexus-bot.service.js`, `bundle.orchestrator.js`, `ebook.generator.service.js`, `logistics.service.js`, `allegro.sentinel.service.js`, `analytics.service.js`), drastycznie przyspieszając pracę bota NeS, rekomendacji zestawów, wycen logistycznych oraz analiz popytu.

23. **[NGINX PROXY 502 BAD GATEWAY FIX]** Naprawa przekierowania ruchu API & WebSockets w szablonie Nginx (`frontend/nginx-https.tpl`):
    - **Diagnoza błędu HTTP 502:** W szablonie `nginx-https.tpl` dyrektywy `proxy_pass` kierowały ruch na nazwę `http://backend:3001`, która ma zastosowanie tylko w sieci kontenerów Docker Compose, natomiast na VPS OVH proces PM2 działa bezpośrednio na hoście `127.0.0.1:3001`.
    - **Naprawa:** Podmieniono `http://backend:3001` na `http://127.0.0.1:3001` dla lokacji `/api/` oraz `/api/socket.io/`, likwidując błędy **HTTP 502 Bad Gateway** i zrywanie kanałów asynchronicznych WebSockets.

24. **[NGINX WEB ROOT & VHOST ACTIVATION FIX]** Przywrócenie wyświetlania aplikacji React Single Page App (`nginx-https.tpl` & `.github/workflows/deploy.yml`):
    - **Diagnoza "Welcome to nginx!":** Szablon `nginx-https.tpl` wskazywał ścieżkę `root /usr/share/nginx/html` (domyślna strona NGINX po zainstalowaniu), zamiast produkcyjnego katalogu zbudowanej aplikacji React `/var/www/nexus/frontend/dist`. Ponadto pipeline deploy nie usuwał domyślnego wariantu `/etc/nginx/sites-enabled/default`.
    - **Naprawa:** Podmieniono `root` na `/var/www/nexus/frontend/dist` oraz zaktualizowano workflow deploymentu o tworzenie symlinka `ln -sf /etc/nginx/sites-available/nexus /etc/nginx/sites-enabled/nexus` i usuwanie `default`, przywracając widoczność aplikacji pod adresem `https://n-e-s.it`.

25. **[EAN PIPELINE ASYNC STATUS & RACE CONDITION FIX]** Naprawa natychmiastowego pomijania pracy Agentów AI (`offer-optimizer.controller.js`):
    - **Diagnoza przedwczesnego `COMPLETE`:** Funkcja `checkPipelineStatus` przy pierwszym odpytaniu HTTP Polling sprawdzała warunek `if (product.offerDraft.title || product.offerDraft.htmlContent)`. Jeśli produkt istniał wcześniej w PostgreSQL z zachowanym starym tytułem, controller natychmiast zwracał `status: COMPLETE` (w 0 sekund), co powodowało zamkniecie ekranu ładowania i wyświetlenie pustych modułów opisowych przed wygenerowaniem treści przez Agentów AI.
    - **Naprawa:** W `triggerUltimatePipeline` zastosowano bezwzględny `upsert` nadpisujący status na `PROCESSING`. W `checkPipelineStatus` dodano rygorystyczny warunek `hasValidHtml` (wymagający niepustego `htmlContent.opis1` oraz statusu innego niż `PROCESSING`), wymuszając utrzymanie widoku ładowania i oczekiwanie na pełen wynik z powiadomień WebSockets / EAN Pipeline.

26. **[PRISMA SCHEMA UPSERT BRAND VALIDATION FIX]** Naprawa błędu HTTP 500 przy `prisma.product.upsert()` (`offer-optimizer.controller.js`):
    - **Diagnoza błędu Prisma:** Operacja `upsert` w `triggerUltimatePipeline` wywoływała błąd walidacji schmatu Prisma `Argument brand is missing` z racji wymogu relacji `brandId` przy tworzeniu nowego rekordu od zera.
    - **Naprawa:** Zastąpiono `upsert` bezpieczną kombinacją `findUnique` i `update` tylko dla istniejących w bazie produktów, pozostawiając tworzenie nowych rekordów (z pełnym mapowaniem marek i kategorii) wewnętrznemu potokowi `EanPipelineService.execute(ean)`. Eliminacja błędu HTTP 500.

27. **[AI MODELS ENTERPRISE UPGRADE 2026]** Zgodnie ze snapshotem API 2026 zaktualizowano modele sztucznej inteligencji pod kątem jakości, bezpieczeństwa EU AI Act i niezawodności wywołań (eliminacja HTTP 400 Bad Request przy użyciu `googleSearch` na niekompatybilnych modelach):
    - **Agent OSINT & Opinie:** `gemini-3.1-pro-preview-customtools` dla 100% integracji z wyszukiwarką.
    - **Agent Prawny (Compliance):** Autorski dedykowany model `antigravity-preview-05-2026`.
    - **Agent GEO & Tytuł & Reszta Systemu:** Ewolucja z szybkiego `gemini-3.5-flash` na stabilny, wolny od halucynacji `gemini-3.1-pro-preview`.
    - **Agent Wizyjny (Vision):** `gemini-3-pro-image`.
    - **Plik referencyjny:** `docs/adr/ADR-021-model-upgrade-2026.md`.

**Uwagi:**
- BaseLinker Token jest w tabeli `SystemSetting` (NIE w .env) — wymaga dostępności Supabase z nowego IP OVH.
- Frontend API_URL pattern: `import.meta.env.PROD ? '' : 'http://localhost:3001'` — w PROD używa relative paths (Nginx proxy).
- Serwer na OVH uruchamiany przez PM2 (nie Docker) — pipeline deploy.yml.

## 2026-07-22 - Poprawa błędów Sockets i Prisma
- Zmodyfikowano frontend/src/* w celu poprawy ERR_NAME_NOT_RESOLVED (API_URL fallback na window.location.origin dla socket.io)
- Zmodyfikowano src/middlewares/auth.middleware.js, by zwracał 503 (zamiast 500) przy problemach z bazą Prisma podczas weryfikacji JWT.

## 2026-07-22 - Dodanie precyzyjnego logowania na logowaniu
- Zmodyfikowano frontend/src/App.jsx: dodano wyświetlanie dokładnego błędu serwera w alercie podczas błędu logowania (500).

## 2026-07-22 - CORS: Dodanie staging.n-e-s.it do whitelisty
- **[BUGFIX - CORS]** Dodano `https://staging.n-e-s.it` do hardkodowanej listy `allowedOrigins` w `src/server.js:81`. Brak tego originu powodował błąd 500 "Niedozwolone origin" na każdym zapytaniu HTTP z przeglądarki na środowisku testowym. Zmienna `CORS_ORIGINS` w `.env` nadal nadpisuje tę listę jeśli jest ustawiona.

## 2026-07-23 - GEO Agent: Naprawa propagacji błędów + Frontend Polling
- **[BUGFIX - GEO AGENT]** `generateGEOTextContent` w `ai.service.js:1049-1052`: Zamieniono cichy fallback (`return {htmlContent: {opis1: "Błąd systemu GEO"}}`) na `throw new Error(...)`. Poprzedni kod powodował, że pipeline traktował awarię agenta jako sukces i zapisywał śmieci do `offerDraft` w bazie, blokując dalsze generowanie dla danego EAN.
- **[BUGFIX - FRONTEND POLLING]** `ImageUploadBox.jsx:34-48`: Dodano obsługę `status === 'ERROR'` w HTTP polling fallback. Bez tego fixa, gdy WebSocket był niedostępny (np. CORS), użytkownik widział wieczny spinner zamiast komunikatu o błędzie.

## 2026-07-23 - Architektura: Dekompozycja Agenta PIM
- **[EAN PIPELINE & PIM]** Wdrożono dekompozycję pobierania twardych parametrów dla słowników Allegro, zdejmując ten proces z Agenta OSINT. W `allegro.service.js` dodano natywne mapowanie katalogowe `getProductParametersByEan`. Agent Autofill przeszedł na tani, zapasowy model `gemini-2.5-flash-lite` z okrojonym o wielkie słowniki promptem. Od teraz Agent 11 pełni jedynie rolę Ostatniej Linii Wsparcia dla szczątkowych braków (oszczędność ~90k tokenów na wywołanie).
- **[BUGFIX - PIM AUTO-FILL]** Zaktualizowano endpoint `/api/products/:id/autofill-params` (wywoływany ręcznie z interfejsu GUI) w `src/server.js`, podłączając w nim pobieranie parametrów twardych z Katalogu Allegro. Bez tej zmiany przycisk ignorował całą warstwę natywną i polegał wyłącznie na zubożałym modelu Lite, który zwracał puste parametry ze względu na limit OSINT. Zoptymalizowano prompt Agenta Lite w `ai.service.js` by był bardziej "łaskawy" i zawsze estymował wymagane cechy po pierwszych wynikach Search.
- **[BUGFIX - NGINX 413 Payload Too Large]** Dodano limit `client_max_body_size 50M;` do plików konfiguracyjnych NGINX (`nginx.conf`, `nginx-http.tpl`, `nginx-https.tpl`), aby umożliwić bezproblemowy zapis dużych obiektów do bazy Prisma (np. treści AEO z osadzonym HTML/Base64), blokowanych wcześniej przez domyślny limit 1 MB dla żądań POST/PATCH na `/api/products/:id`.
- **[BUGFIX - TELEMETRIA]** Przywrócono pierwotny tag zliczający tokeny (telemetryczny) z "Agent_11_Autofill_Lite" na bazowe "Agent_11_Autofill" w pliku `ai.service.js`. Powstrzymało to ignorowanie przez dashboard telemetrii zużycia nowego taniego modelu i przywróciło pełny monitoring zużycia dla tego modułu na starym kanale.

## 2026-07-23 - Bezpieczeństwo UX: Globalny przechwytywacz 401
- **[BUGFIX - 401 UNAUTHORIZED]** W `frontend/src/App.jsx` dodano globalny interceptor dla `axios.interceptors.response`. Przechwytuje on wszystkie błędy 401 z backendu i wywołuje `handleLogout` wymuszając wylogowanie z aplikacji. Rozwiązuje to problem blokady interfejsu i wprowadzających w błąd alertów podczas wywołania endpointów (np. `/api/products/:id/sync-category-bl` do pobrania parametrów Allegro PIM po EAN), gdy sesja JWT wygasła.

## 2026-07-23 - Zmiana Strategii Rozgałęzień
- **[WORKFLOW - HOTFIX MODE]** Zgodnie z decyzją użytkownika, zaktualizowano `AGENTS.md`. Wszystkie prace (do odwołania) prowadzone będą bezpośrednio na gałęzi `main` z pominięciem środowiska `dev`. Zmiany są automatycznie wypychane (`push`) prosto na produkcję w celu skrócenia czasu przywracania sprawności systemu.

## 2026-07-23 - PIM Auto-Fill: Odbudowa mechanizmu i stabilizacja Agenta
- **[BUGFIX - AI AGENT]** W `ai.service.js` zmieniono model z wycofanego przez Google `gemini-2.5-flash-lite` (który zwracał błąd `404 Not Found` blokując cały endpoint `/autofill-params`) na oficjalny `gemini-3.5-flash`. Model `3.5-flash` jest równie tani, w pełni obsługuje `googleSearch: {}` (co pozwala na realizację zaleceń wyszukiwania bez drogiego Deep Researchu na modelach Pro zjadających 91000 tokenów) i działa stabilnie. Upewniono się, że błędy wywołań API są poprawnie propagowane na zewnątrz (`throw new Error`), co wcześniej pomogło zdemaskować błąd 404. Zaktualizowano też ADR-021.
- **[BUGFIX - LAZY SCHEMAS]** `server.js:468` oraz `ean.pipeline.service.js:98`. Endpoint `autofill-params` omijał Agenta (pusta pętla) w sytuacjach, w których kategoria `allegroCategoryId` została ustawiona w interfejsie, ale jej struktura `parameters` w `marketplaceCategory` była w cache pusta. Wdrożono inteligentne doczytywanie słownika parametryzacji `fetchCategoryParameters(catId)` z API Allegro tuż przed odpaleniem AI. Zapewniono też fallback na wyszukanie samej kategorii dla nowych draftów.
- **[BUGFIX - AEO TELEMETRY]** W `server.js` na endpointcie `POST /api/products/:id/aeo` usunięto dziedzictwo starego serwisu `core/ai.service` (który nie wspierał bazy metryk z ADR 022) i podpięto w to miejsce oficjalny mechanizm wieloagentowy `modules/offer-optimizer/ai.service.js` -> `generateAEOContent`. Dodano też wewnątrz wywołania funkcji twardy tag telemetryczny `"Agent_AEO"`, aby w panelu telemetrycznym zawsze logowało się zużycie API dla tego agenta.

 # #   2 0 2 6 - 0 7 - 2 3   -   W d r o |e n i e   s c e n t r a l i z o w a n e j   t e l e m e t r i i   d l a   A g e n t � w   ( S t a g e   3 ) 
 -   * * [ T E L E M E T R Y ] * *   Z a k o Dc z o n o   p e Bn   m i g r a c j   a g e n t � w   a n a l i t y c z n y c h   i   s p o Be c z n o [c i o w y c h   d o   s c e n t r a l i z o w a n e g o   s y s t e m u   [l e d z e n i a   k o s z t � w   i   t o k e n � w   ( \ g e n e r a t e W i t h R e t r y \   - >   \ A i M e t r i c s S e r v i c e \ ) . 
 -   Z i n t e g r o w a n o   \ A g e n t _ D a t a _ A n a l y s t \   z   u |y c i e m   \ g e m i n i - 3 . 5 - f l a s h \   w   m o d u l e   a n a l i t y c z n y m   ( p r o g n o z o w a n i e   p o p y t u ) . 
 -   Z i n t e g r o w a n o   \ A g e n t _ V e c t o r _ E m b e d d i n g \   i   \ A g e n t _ H u n t e r \   w   m o d u l e   i n f l u e n c e r � w ,   p r z e c h o d z c   n a   z a a w a n s o w a n y   m o d e l   \ 	 e x t - e m b e d d i n g - 0 0 4 \   d l a   e m b e d d i n g � w   t e k s t o w y c h   o r a z   w d r o |o n o   m a n u a l n e   l i c z e n i e   t o k e n � w   ( a p p r o x )   n a   b a z i e   w a g i   t e k s t u . 
 -   Z i n t e g r o w a n o   \ A g e n t _ S o c i a l _ I n t e g r a t i o n \   w   i n w i g i l a c j i   k o n t   z   u |y c i e m   \ g e m i n i - 3 . 1 - p r o - p r e v i e w \ . 
 -   Z i n t e g r o w a n o   \ A g e n t _ N e x u s _ B o t \   p o p r z e z   r o z s z e r z e n i e   c h a t b o t a   N e S   o   z r z u t   \ u s a g e M e t a d a t a \   z   j e g o   g B� w n e g o   m o d e l u   p o   z a k o Dc z e n i u   c a Be j   k o n w e r s a c j i   ( w Bc z a j c   w   t o   z u |y c i e   d l a   T o o l s / F u n c t i o n   C a l l i n g ) . 
 
 
 

## 2026-07-23 - Wdrożenie Supervisor Agenta i bazy RAG (pgvector)
- **[EAN PIPELINE]** Zastąpiono starą sekwencyjną logikę (w ean.pipeline.service.js) nowym mechanizmem kolejkowym obsługiwanym przez SupervisorService. Potok EAN dodaje zadanie EAN_PIPELINE do AgentQueue.
- **[SUPERVISOR]** Utworzono supervisor.service.js, który jako inteligentny Agent nadzorca (gemini-3.1-pro-preview) decyduje o kolejności wywołań poszczególnych pod-agentów (Autofill, AEO, OSINT, itp.) dla danego zadania na podstawie analizy PIM, optymalizując wywołania API i sprawdzając AgentCache.
- **[RAG]** Zaimplementowano wtyczkę pgvector do Postgresa (Supabase) oraz nową usługę knowledge.rag.service.js, aby wyposażyć Supervisor Agenta we własną Bazę Wiedzy (np. o dyrektywie Omnibus). Utworzono frontendowy panel KnowledgeBasePanel.jsx do dodawania dokumentów txt/md i wektoryzacji (text-embedding-004).

## Modul: Unified Product Pipeline (PIM & Agent Supervisor)
**Data:** 2026-07-24
**Zmiany:**
1. **[REFAKTORYZACJA UI]** UsuniÄ™to 'gigantyczny modal' PIM z App.jsx. Utworzono nowy komponent `UnifiedProductPipelineView.jsx`.
2. **[GRID LAYOUT]** Podzielono widok na dwie kolumny: Lewa (Dane PIM, Ceny, CĹ‚o, Gabaryty, Kategoria, Galeria) i Prawa (Supervisor Agent - TytuĹ‚, StrictWysiwyg, Audyt Wizyjny).
3. **[PRZYCISK AEO]** Przeniesiono logikÄ™ 'Generuj AEO' z tabeli `ProductsView.jsx` bezpoĹ›rednio do lewej kolumny (PIM) w nowym widoku.
4. **[ZAKĹADKA UNIFIED]** Zmodyfikowano routing w `App.jsx`, dodajÄ…c stan `activeTab === 'unifiedHub'`, pozwalajÄ…cy na wejĹ›cie do nowego widoku po klikniÄ™ciu 'Nowe SKU' lub edycji w `ProductsView.jsx`.
