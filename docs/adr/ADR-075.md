# ## Modul: MTool HQ - Generator Kart SDS (UE 2020/878) - Eliminacja Zafałszowania Daty Producenta i Naprawa Ekstrakcji Nagłówków RTF (ADR-075)
**Data:** 2026-09-15
**Zmiany:**
1. **[PARSER RTF: ODBLOKOWANIE EKSTRAKCJI NAGŁÓWKÓW (`SDSRTFParser`)]**
   - Usunięto tokeny `header`, `headerr`, `headerl`, `headerf` z tablicy `IGNORABLE_GROUPS`.
   - Zapewniono pełny odczyt bloków nagłówkowych RTF, eliminując problem utraty numeru rewizji oraz daty producenta (`Revision nr. 2`, `Dated 14/02/2025`, `Replaced revision:1 (Dated: 01/09/2023)`).
2. **[SYSTEMOWY ROZDZIAŁ METRYKI POLSKIEJ OD DAT PRODUCENTA (UE 2020/878)]**
   - **Data sporządzenia:** Wskazuje datę opracowania polskiej karty (data bieżącej adaptacji, np. `15.09.2026`).
   - **Aktualizacja:** Dla wydania pierwszego (`1.0 PL`) przyjmuje wartość `"Nie dotyczy"` (karta nie była wcześniej aktualizowana w języku polskim).
   - **Zastępuje wersję:** Dynamicznie wskazuje wyekstrahowaną ze źródła datę producenta (`${originalDate}`). Całkowicie wyeliminowano fallback `new Date()` z daty producenta.
   - **Zero Hardcodów:** W `SDSDocxExporter` usunięto sztywny string z datą 14.02.2025 – silnik obsługuje dowolną kartę i dowolną datę producenta.
3. **[RYGORYSTYCZNE ASERCJE XML W TESTACH]**
   - W `tests/sds.8_points_audit.test.js` oraz `tests/sds.rtf.test.js` wdrożono bezpośrednie asercje na strukturę `word/document.xml`, sprawdzające datę sporządzenia PL, `Aktualizacja: Nie dotyczy`, dynamiczną datę producenta w `Zastępuje wersję` oraz wykluczające pojawienie się daty systemowej w klauzuli producenta.
   - Zsynchronizowano pliki `docs/SDS/Karta_Charakterystyki_8034055535448_SDS_ORCHIDEA_E_VANIGLIA (9).docx`, `(12).docx` oraz `_V2_PL.docx`.
4. **[STATUS WERYFIKACJI]**
   - 12/12 testów audytu (`tests/sds.8_points_audit.test.js`): PASSED.
   - 6/6 testów RTF (`tests/sds.rtf.test.js`): PASSED.
   - 7/7 testów prawnych (`tests/sds.compliance.test.js`): PASSED.
   - 122/122 testów systemowych (`npm test`): PASSED.
   - Zweryfikowano poprawność na syntetycznej karcie z inną datą (05/11/2023) – zero fałszywych dopasowań czy pozostałości po innych kartach.
