# ## Modul: MTool HQ - Generator Kart SDS (UE 2020/878) - Pełna Zgodność Prawno-Chemiczna, Eliminacja Wycieków Obcojęzycznych i Rygorystyczny Audyt (ADR-065)
**Data:** 2026-09-14
**Zmiany:**
1. **[ELIMINACJA WYCIEKÓW JĘZYKOWYCH W SEKCJI 9 (PARAMETRY FIZYKOCHEMICZNE)]**
   - W `normalizePhysChemValue` wdrożono bezwzględne tłumaczenie zwrotów wielowyrazowych i stanów skupienia przed pojedynczymi słowami: `flammable liquid` -> `ciecz łatwopalna`, `Method:not specified` -> `(metoda: nie określono)`, `Method: internal` / `internal` -> `(metoda wewnętrzna)`, `Median equivalent diameter` -> `Nie dotyczy (produkt płynny)`.
   - Zaimplementowano urzędowe nagłówki i treść dla Sekcji 9.2: `9.2.1. Informacje dotyczące klas zagrożenia fizycznego` oraz `9.2.2. Inne właściwości bezpieczeństwa` z domyślnym urzędowym zapisem braku dodatkowych danych badawczych.
2. **[SCENTRALIZOWANA I HERMETYCZNA TRANSLACJA NARRACYJNA (sds.agent.js)]**
   - W `prepareAgentPayload` włączono sekcje narracyjne: `section_1_2`, `section_4`, `section_5`, `section_6`, `section_7`, `section_10`, `section_11` do pakietu `descriptiveSectionsToTranslate`.
   - Wprowadzono rygorystyczny `SYSTEM_PROMPT` dla modelu LLM nakazujący 100% polszczyzny medyczno-BHP, zakaz stosowania skrótów i placeholderów oraz nienaruszalność danych liczbowych.
   - W `mergeCompletedSds` dodano `polonizeToxicologicalSection` polonizujący nazwy gatunków badawczych (szczur, królik, mysz, świnka morska), dróg podania (droga pokarmowa, na skórę, inhalacyjnie) oraz naprawiający literówki OCR (`DRACOWNICY` -> `PRACOWNICY`).
3. **[ODPORNOŚĆ SEKCJI 14 NA ARTEFAKTY PAGINACJI I STOPKI]**
   - W `processSection14` kod ograniczeń przewozu przez tunele zabezpieczono ścisłym regexem ADR `(\([A-E](?:\/[A-E])?\)|\b[A-E](?:\/[A-E])?\b)`, eliminując wklejanie stopki producenta `Suarez Company`.
4. **[BEZSTRATNY MOSTEK WORD COM DLA PLIKÓW RTF]**
   - Zintegrowano moduł `SDSRtfConverter` z Microsoft Word COM w środowisku Windows, umożliwiający bezbłędną i bezstratną konwersję plików `.rtf` do `.pdf` z zachowaniem 100% układu tabel LIMS (np. Orchidea e Vaniglia).
5. **[BRAMKI JAKOŚCI I AUDYT ZEWNĘTRZNY]**
   - Utworzono i zweryfikowano skrypt audytorski `scratch/audit_docx.js` badający zawartość generowanych plików `.docx` pod kątem jakichkolwiek obcojęzycznych zwrotów. Obie karty (Orchidea - RTF oraz Najma - PDF) uzyskały wynik: **0 błędów obcojęzycznych**.
   - Zestaw testów regulacyjnych `tests/sds.compliance.test.js` (7/7 PASSED), testy formatu RTF `tests/sds.rtf.test.js` (6/6 PASSED) oraz pełny zestaw systemowy `npm test` (122/122 PASSED) potwierdzają 100% stabilność i zero regresji.
