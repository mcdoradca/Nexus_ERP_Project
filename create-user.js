const bcrypt = require('bcrypt'); const { PrismaClient } = require('@prisma/client'); const prisma = new PrismaClient(); async function main() { const hash = await bcrypt.hash('test1234', 10); await prisma.user.upsert({ where: { email: 'agent@test.com' }, update: { passwordHash: hash }, create: { email: 'agent@test.com', passwordHash: hash, name: 'Agent', role: 'ADMIN', department: 'PREZES', group: 'PRACOWNICY', color: 'bg-red-500 text-white' } }); console.log('User created'); } main().catch(console.error).finally(()=>prisma.$disconnect());
